import colors from 'tailwindcss/colors'

export default {
  theme: {
    extend: {
      colors: {
        primary: colors.teal,
        backgroundMain:  colors.gray[800]
      },
      fontFamily: {
        main: ['Oxanium', 'sans-serif'], 
      },
    }
  }
}
